services:
  - type: web
    name: karma
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd app && python server_fast.py"
    envVars:
      - key: PORT
        value: 3000
