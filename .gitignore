app/package.json
app/server.js
.DS_Store

.claude/settings.local.json

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Quellfotos der Karten: nur die optimierten WebP gehen ins Repo, die
# hochauflösenden Originale bleiben lokal.
Bilder/originale/
app/public/Bilder/fotos/*.png
app/public/Bilder/fotos/*.jpg
app/public/Bilder/fotos/*.jpeg

# Ablage für ein laufendes Karten-Update (Originalfotos + Excel-Vorgabe).
# Was davon gebraucht wird, landet als WebP bzw. in Karten/Karten.csv im Repo.
_update/
